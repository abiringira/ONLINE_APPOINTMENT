var expect = require("chai").expect;
var request = require("request");

describe("Status and content", function() {
  it("should do at least one test", done => {
    // expect(1).toBe(1);
    done();
  });
});
