<!--
including header content which is common for all pages
-->
<%- include ../layouts/header.ejs %>

<% if (messages.error) { %>
	<div id="toast" class="toast toast-error">
			<button id="btn-clear" class="btn btn-clear float-right"></button>
			<%- messages.error %>
		</div>
<% } %>

<% if (messages.success) { %>
	<div id="toast" class="toast toast-success">
			<button id="btn-clear" class="btn btn-clear float-right"></button>
			<%- messages.success %>
		</div>
<% } %>
<div class="content-wrapper--outer padded">
	
	<div class="content-wrapper--inner centerize padded-tb">
	<div class="heading text-center">
		<h3>Doctors List</h3>
	</div>
	<div class="divider"></div>
	<table class="table table-striped table-hover">
			<thead>
			  <tr>
				<th>First Name</th>
				<th>Last Name</th>
				<th>Mobile</th>
				<th>Email</th>
				<th>Gender</th>
				<th>ID</th>
				<th>Username</th>
				<th>Center</th>
				
			  </tr>
			</thead>
			<tbody>
			<% if (userData) { %>
			<% userData.forEach(function(user){ %>
				<tr>
					<td class="text-capitalize"><%= user.firstName %></td>
					<td class="text-capitalize"><%= user.lastName %></td>
					<td><%= user.mobile %></td>
					<td><%= user.email %></td>
					<td class="text-capitalize"><%= user.gender %></td>
					<td><%= user.nid %></td>
					<td class="text-capitalize"><%= user.username %></td>
					<td class="text-capitalize"><%= user.center %></td>
					<td>
						<!-- <div style="float:left">
							<a href='/users/edit/<%= user._id %>'>Edit</a> &nbsp;							
							<form method="post" action="/users/delete/<%= user._id %>" style="float:right">
								<input type="submit" name="delete" value='Delete' onClick="return confirm('Are you sure you want to delete?')" />
								<input type="hidden" name="_method" value="DELETE" />
							</form>
						</div> -->
					
						<!-- <button onClick="return confirm('Are you sure you want to delete?')" class="btn btn-error"><i class="icon icon-delete"></i></button> -->
						<form method="post" action="/users/<%= user._id %>?_method=PATCH">
                            
                            <button type="submit" class="btn btn-action btn-primary"  ><i class="icon icon-edit"></i></a>
                            </form>
                                <form method="post" action="/users/<%= user._id %>?_method=DELETE">
							<button type="submit"  class="btn btn-action btn-error"><i class="icon icon-delete"></i></button>
							
						</form>
					</td>
				</tr>
				<% }) %>
				<% } %>
			</tbody>
		  </table>
		  </div>
		   
</div>

<!--
including footer content which is common for all pages
-->
<%- include ../layouts/footer.ejs %>
